export type Currency = "UYU" | "USD" | "UI";
export type TransactionKind = "income" | "expense" | "transfer";
export type TransferPurpose = "regular" | "savings" | "investment";
export type Session = { version?: number; access_token: string; refresh_token: string; token_type: "bearer"; expires_in: number; user: { id: number; email: string } };
export type Account = { id: number; name: string; currency: Currency; opening_balance: string; current_balance: string; is_active: boolean; adjustments: { id: number; date: string; amount: string; note: string }[] };
export type AccountInput = { name: string; currency: Currency; opening_balance: string };
export type Category = { id: number; name: string; kind: TransactionKind; parent_id: number | null; is_active: boolean };
export type Transaction = { id: number; date: string; kind: TransactionKind; amount: string; destination_amount: string | null; purpose: TransferPurpose | null; description: string; notes: string | null; account_id: number; destination_account_id: number | null; category_id: number | null; category_source: string | null; is_voided: boolean; created_at: string };
export type TransactionPage = { items: Transaction[]; total: number; page: number; page_size: number };
export type MovementFilters = { month?: string; date_from?: string; date_to?: string; kind?: TransactionKind; account_id?: number; category_id?: number; currency?: Currency; search?: string; include_voided?: boolean; page?: number; page_size?: number };
export type RecurringExpense = { id: number; description: string; amount: string; day_of_month: number; account_id: number; category_id: number | null; is_active: boolean };
export type MonthlyBudget = { currency: Currency; amount: string | null };
export type HistoryMonth = { month: string; income: string; expenses: string; net: string };
export type HistoryReport = { currency: Currency; months: HistoryMonth[] };
export type MonthlyReport = { month: string; currency: Currency; income: string; expenses: string; net: string; savings: string; spent_percentage: string | null; budget: string | null; comparison?: { previous_month: string; previous_expenses: string; change_percentage: string | null }; categories: { category_id: number | null; name: string; amount: string; percentage: string | null }[]; income_sources?: { name: string; amount: string; percentage: string }[]; top_expenses?: Transaction[]; recent_transactions: Transaction[]; insights?: { type: string; title: string; detail: string }[] };
export type Reconciliation = { account: Account; calculated_balance: string; actual_balance: string; adjustment: { id: number; date: string; amount: string; note: string } | null; already_reconciled: boolean };
export type TransactionInput = { date: string; kind: TransactionKind; amount: string; description: string; account_id: number; category_id?: number | null; destination_account_id?: number | null; destination_amount?: string | null; purpose?: TransferPurpose | null; notes?: string | null };
